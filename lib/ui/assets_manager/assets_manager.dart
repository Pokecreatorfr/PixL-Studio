import 'package:flutter/material.dart';


class AssetsManager extends StatelessWidget {
  const AssetsManager({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: const Center(
        child: Text('Assets Manager'),
      ),
    );
  }
}